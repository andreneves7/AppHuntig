# TEMPLATE — copia este ficheiro para "keystore.properties" (na raiz do
# projeto, ao lado deste) e preenche com os teus valores reais.
# O ficheiro "keystore.properties" (sem .example) está no .gitignore —
# nunca vai para o Git. Este .example fica, é só a estrutura, sem segredos.
#
# Ver docs/RELEASE_CHECKLIST.md secção 2 para os passos completos de
# como criar a keystore.

storeFile=../apphuntig-release.jks
storePassword=SUBSTITUI_PELA_TUA_PASSWORD_DA_KEYSTORE
keyAlias=apphuntig
keyPassword=SUBSTITUI_PELA_TUA_PASSWORD_DA_CHAVE
